---
# 所有能配图的位置都在这里 —— 想换图就改右边。
# 用法：把图片文件丢进 _data/images/ 文件夹，然后右边写它的文件名（如 me.jpg）。
#   也可以写 /xxx 指向 public/ 里的图，或 http 外链；留空 "" 则显示空画框。
icon: "/naive/icon.svg"         # 浏览器标签页小图标（favicon），默认是日语假名「な」
home_hero: "portrait.jpeg"      # 首页右侧大图
about_portrait: "portrait.jpeg" # 关于页肖像
---

文章封面图不在这里 —— 在每篇文章自己的 image: 字段里设（每篇一张）。
换 favicon：把你的图标丢进 _data/images/，再把上面 icon 改成那个文件名即可。
